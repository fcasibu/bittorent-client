{
    "name": "bittorent-client",
    "version": "1.0.0",
    "main": "dist/index.js",
    "scripts": {
        "build": "rimraf dist && tsc",
        "format": "prettier . --cache --cache-location ./.prettier-cache --cache-strategy content --write",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "author": "fcasibu",
    "license": "ISC",
    "devDependencies": {
        "@types/node": "^20.14.2",
        "prettier": "^3.3.1",
        "rimraf": "^5.0.7",
        "typescript": "^5.4.5"
    }
}
